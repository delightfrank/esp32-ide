# ESP32 IDE Bug 修复验收方案

**目标**: 修复全部 25 项 Bug，确保数据安全、运行稳定、功能完整
**总工作量**: 35.5 人时
**创建时间**: 2026-08-19 16:30

> **核心原则**: 每阶段完成 → 验收测试 → 确认无 bug → 再进入下一阶段

## Bug 总览
- P0 数据丢失: 5 项（必须修）
- P1 稳定性: 7 项（高优先）
- P2 功能缺陷: 6 项
- P3 UX/安全: 7 项

---
## Phase 1: P0 数据丢失修复
**目标**: 消除所有数据丢失风险，确保用户代码安全
- ⚠️ 自动保存频率过高可能影响性能

### 1.1 自动保存机制
- **描述**: 每 30 秒自动保存到 .autosave 文件，切换文件/编译前也触发
- **工作量**: 4h
- **验收标准**:
  - [ ] 编辑器修改后 30 秒内自动保存
  - [ ] 切换文件前自动保存
  - [ ] 编译/烧录前自动保存
  - [ ] .autosave 文件在项目目录下

### 1.2 崩溃恢复
- **描述**: 启动时检测 .autosave，弹窗提示用户恢复
- **工作量**: 3h
- **前置依赖**: 自动保存机制
- **验收标准**:
  - [ ] IDE 重启后弹窗提示'发现未保存的恢复文件'
  - [ ] 用户选择恢复后内容正确加载
  - [ ] 用户选择忽略后删除 .autosave

### 1.3 IPC 超时保护
- **描述**: 给 get-editor-content-response 加 5 秒超时
- **工作量**: 2h
- **验收标准**:
  - [ ] 保存操作 5 秒内无响应自动失败
  - [ ] 不阻塞 UI 线程
  - [ ] 失败后弹窗提示用户

### 1.4 文件切换保存逻辑修复
- **描述**: handleFileOpenFromTree 中 await saveFile 结果
- **工作量**: 1h
- **前置依赖**: IPC 超时保护
- **验收标准**:
  - [ ] 切换文件时正确保存当前文件
  - [ ] 保存失败时不切换文件

---
## Phase 2: P1 稳定性修复
**目标**: 消除进程残留、引用过时、主线程阻塞等问题
- ⚠️ 子进程清理需要跨模块协调

### 2.1 编译进程超时
- **描述**: runPio 添加 10 分钟超时，超时自动 kill
- **工作量**: 2h
- **验收标准**:
  - [ ] 编译超过 10 分钟自动终止
  - [ ] 超时后显示错误提示
  - [ ] isBuilding 状态正确重置

### 2.2 IDE 退出清理子进程
- **描述**: app.on('before-quit') 中 kill 所有活跃进程
- **工作量**: 2h
- **验收标准**:
  - [ ] 关闭 IDE 后 pio/gcc 进程不存在
  - [ ] 任务管理器无残留

### 2.3 mainWindow 引用动态获取
- **描述**: registerPioIpc/serial 改为闭包获取最新窗口
- **工作量**: 2h
- **验收标准**:
  - [ ] 窗口重建后编译/串口功能正常
  - [ ] 不再使用注册时的旧引用

### 2.4 目录列表异步化
- **描述**: file-tree.js 改用 fs.promises.readdir + 分批处理
- **工作量**: 3h
- **验收标准**:
  - [ ] 大项目文件树不卡 UI
  - [ ] 1000+ 文件项目响应时间 < 2 秒

### 2.5 目录监听增强
- **描述**: fs.watch 失败时 fallback 到 fs.watchFile
- **工作量**: 2h
- **验收标准**:
  - [ ] 网络驱动下文件树仍能刷新
  - [ ] 监听失败不崩溃

### 2.6 串口重连时序修复
- **描述**: monitorPostUpload 保存端口+波特率到变量，不依赖渲染进程
- **工作量**: 2h
- **验收标准**:
  - [ ] 烧录后自动重连正确的串口和波特率
  - [ ] 不出现重连错误端口

---
## Phase 3: P2 功能缺陷修复
**目标**: 修复串口检测、配置覆盖、日志内存等问题

### 3.1 串口检测改用 scan 模式
- **描述**: checkPortAvailable 改为只查询不打开
- **工作量**: 2h
- **验收标准**:
  - [ ] 检测串口占用时不抢占串口
  - [ ] 烧录和检测可同时进行

### 3.2 platformio.ini 覆盖保护
- **描述**: 生成前检测是否已存在，存在则弹窗确认
- **工作量**: 1h
- **验收标准**:
  - [ ] 已有 ini 时弹窗询问是否覆盖
  - [ ] 用户取消则不覆盖

### 3.3 输出日志限制
- **描述**: outputLines 最多保留 5000 行，超出自动截断
- **工作量**: 1h
- **验收标准**:
  - [ ] 大项目编译后内存不爆
  - [ ] 最新的日志始终保留

### 3.4 文件树目录过滤
- **描述**: 增加 .pio/.pioenvs/.piolibdeps 过滤
- **工作量**: 1h
- **验收标准**:
  - [ ] .pio 构建目录不出现在文件树中

### 3.5 build 闭包修复
- **描述**: handleBuild/handleUpload 使用 ref 获取最新 projectPath
- **工作量**: 2h
- **验收标准**:
  - [ ] 选择项目后立即编译不报错
  - [ ] 不需要重启 IDE

---
## Phase 4: P3 UX 与安全修复
**目标**: 路径检测、编码支持、安全加固

### 4.1 中文路径检测
- **描述**: 启动时检测 app 路径和项目路径是否含中文/空格
- **工作量**: 1h
- **验收标准**:
  - [ ] 路径含中文时弹窗警告
  - [ ] 建议用户移动到纯英文路径

### 4.2 文件编码检测
- **描述**: 打开文件时尝试 UTF-8，失败则用 GBK 解码
- **工作量**: 2h
- **验收标准**:
  - [ ] GBK 文件正确显示中文
  - [ ] 保存时保持原编码

### 4.3 preload 安全加固
- **描述**: 移除 fileWrite，改为只通过 save-file 保存
- **工作量**: 1h
- **验收标准**:
  - [ ] 渲染进程无法直接写任意文件
  - [ ] 文件保存只能通过菜单/快捷键

### 4.4 deleteItem 异常处理
- **描述**: file-tree.js deleteItem 添加 try/catch
- **工作量**: 0.5h
- **验收标准**:
  - [ ] 删除失败不崩溃，返回错误信息

### 4.5 串口监视器时间戳
- **描述**: 每条数据前显示 HH:MM:SS.mmm 时间戳
- **工作量**: 1h
- **验收标准**:
  - [ ] 接收数据显示时间戳
  - [ ] 时间戳可选开关
