# ESP32 便捷 IDE —— 分阶段实施方案

> 原则：每阶段完成 → 验收测试 → 确认无 bug → 再进入下一阶段

---

## Phase 1：Electron 脚手架 + 编辑器基础

**目标：** 跑起来一个 Electron 应用，能打开、编辑、保存代码文件

### 1.1 交付物
- Electron 项目初始化完成
- Monaco Editor 集成，支持 C/C++ 语法高亮
- 基础菜单栏（文件：新建/打开/保存）
- 窗口标题显示当前文件名

### 1.2 验收标准
| # | 测试项 | 通过条件 |
|---|--------|----------|
| 1 | 应用启动 | 双击 exe 后 3 秒内显示窗口，无白屏 |
| 2 | 新建文件 | Ctrl+N 新建空白文件，可输入代码 |
| 3 | 打开文件 | Ctrl+O 选择 .c/.cpp/.h 文件，内容正确显示 |
| 4 | 保存文件 | Ctrl+S 保存，重新打开内容一致 |
| 5 | 语法高亮 | 输入 `#include <stdio.h>` 等 C 代码，关键字有颜色区分 |
| 6 | 窗口行为 | 可拖动、可缩放、最小化/最大化/关闭正常 |
| 7 | 无崩溃 | 连续操作 10 分钟，无闪退、无无响应 |

### 1.3 验收方式
手动测试全部 7 项，截图/录屏记录，全部通过后签字进入 Phase 2

---

## Phase 2：PlatformIO 集成 + 编译功能

**目标：** 能在 IDE 内一键编译 ESP32-S3 项目

### 2.1 交付物
- PlatformIO Core 嵌入或可调用
- 编译按钮 + 编译输出面板
- 自动生成 `platformio.ini`（选芯片后）
- 编译日志实时显示（滚动输出）
- 编译成功/失败状态提示

### 2.2 验收标准
| # | 测试项 | 通过条件 |
|---|--------|----------|
| 1 | PlatformIO 可调用 | 点击编译后，后端正确执行 `pio run` |
| 2 | 自动生成配置 | 选择 ESP32-S3 后，`platformio.ini` 内容正确 |
| 3 | 编译输出 | 输出面板实时显示编译日志，不卡顿 |
| 4 | 编译成功 | Blink 模板项目编译通过，无 error |
| 5 | 编译失败 | 故意写错代码，显示错误信息（行号+原因） |
| 6 | 错误跳转 | 点击编译错误，光标跳转到对应代码行 |
| 7 | 编译按钮状态 | 编译中按钮变灰+显示进度，完成后恢复 |
| 8 | 无残留进程 | 编译完成后无残留 pio/gcc 进程 |

### 2.3 前置条件
- Phase 1 验收通过
- PlatformIO 已安装在开发机（`pio --version` 可用）
- ESP32-S3 工具链已下载（`~/.platformio/packages/` 存在）

### 2.4 验收方式
用 Blink 模板项目走一遍完整编译流程，全部 8 项通过后进入 Phase 3

---

## Phase 3：烧录功能

**目标：** 能一键烧录到 ESP32-S3 开发板

### 3.1 交付物
- 串口自动检测/手动选择
- 烧录按钮 + 烧录进度显示
- 烧录成功/失败提示
- 烧录前自动编译（如果未编译）

### 3.2 验收标准
| # | 测试项 | 通过条件 |
|---|--------|----------|
| 1 | 串口检测 | 连接开发板后，下拉列表自动显示可用串口 |
| 2 | 烧录执行 | 点击烧录，调用 `pio run -t upload` |
| 3 | 烧录成功 | 开发板 LED 闪烁（Blink 程序运行） |
| 4 | 烧录失败 | 拔掉 USB 后烧录，显示"串口未找到"错误 |
| 5 | 进度显示 | 烧录过程中显示百分比或进度条 |
| 6 | 自动编译 | 代码修改后直接烧录，自动先编译再烧录 |
| 7 | 串口占用 | 烧录时其他程序占用串口，提示"串口被占用" |
| 8 | 完整流程 | 新建项目 → 写 Blink 代码 → 编译 → 烧录 → LED 闪烁 |

### 3.3 前置条件
- Phase 2 验收通过
- ESP32-S3 开发板 + USB 线
- CH340/CP2102 串口驱动已安装

### 3.4 验收方式
拿一块 ESP32-S3 开发板，从零开始走完整流程（新建→编译→烧录→验证），全部 8 项通过

---

## Phase 4：串口监视器

**目标：** 能在 IDE 内查看串口输出，发送数据

### 4.1 交付物
- 串口监视器面板（底部 tab）
- 实时显示串口数据
- 支持发送输入
- 波特率选择（9600/115200/自定义）
- 清屏功能

### 4.2 验收标准
| # | 测试项 | 通过条件 |
|---|--------|----------|
| 1 | 打开串口 | 选择串口+波特率后，点击连接，状态显示"已连接" |
| 2 | 接收数据 | 开发板发送数据，监视器实时显示 |
| 3 | 发送数据 | 输入框输入文字+回车，开发板收到数据 |
| 4 | 波特率切换 | 115200→9600 切换后，数据接收正常 |
| 5 | 断开连接 | 点击断开，串口释放，其他程序可正常打开 |
| 6 | 清屏 | 点击清屏，历史数据清除，新数据继续显示 |
| 7 | 自动滚动 | 新数据到来时自动滚动到底部 |
| 8 | 与烧录共存 | 串口监视器打开时，烧录功能正常（自动断开→烧录→重连） |

### 4.3 前置条件
- Phase 3 验收通过
- 开发板运行串口回显程序（Serial echo）

### 4.4 验收方式
开发板跑 echo 程序，监视器收发测试，全部 8 项通过

---

## Phase 5：项目管理 + 模板

**目标：** 新手友好，一键创建项目，文件管理清晰

### 5.1 交付物
- 左侧文件树面板
- 新建项目向导（选芯片 → 选模板 → 命名 → 创建）
- 项目模板：Blink、WiFi 扫描、串口回显
- 删除/重命名文件

### 5.2 验收标准
| # | 测试项 | 通过条件 |
|---|--------|----------|
| 1 | 文件树显示 | 左侧正确显示项目目录结构 |
| 2 | 新建项目 | 向导走完，生成完整项目目录+platformio.ini |
| 3 | 模板内容 | Blink 模板编译通过+烧录后 LED 闪烁 |
| 4 | 文件操作 | 右键新建/删除/重命名文件，文件树实时更新 |
| 5 | 多文件项目 | 新建 .h/.c 文件后，编译正确识别 |
| 6 | 刷新 | 手动在外部修改文件，点刷新后文件树更新 |

### 5.3 前置条件
- Phase 4 验收通过

### 5.4 验收方式
从零创建一个 Blink 项目，验证全流程，全部 6 项通过

---

## Phase 6：打包发布

**目标：** 打包成 Windows 离线安装包，国内可下载

### 6.1 交付物
- Windows 离线压缩包（含所有依赖）
- 使用说明文档（README）
- Gitee Release 发布页面
- 百度网盘/蓝奏云备份链接

### 6.2 验收标准
| # | 测试项 | 通过条件 |
|---|--------|----------|
| 1 | 包体积 | ≤ 500MB（目标 400MB） |
| 2 | 无联网依赖 | 断网状态下解压→启动→编译→烧录，全流程正常 |
| 3 | 全新机器 | 在未装任何开发环境的 Windows 10/11 上测试 |
| 4 | 下载速度 | 国内网络下 Gitee 下载速度 ≥ 1MB/s |
| 5 | 解压即用 | 解压后双击 exe 直接运行，不需要安装 Python 等 |
| 6 | 文档完整 | README 包含：功能介绍、下载地址、使用教程、常见问题 |
| 7 | 卸载 | 删除整个文件夹即可卸载，无残留注册表/环境变量 |
| 8 | 分发渠道 | Gitee Release 页面可正常访问和下载 |

### 6.3 前置条件
- Phase 1-5 全部验收通过
- Windows 打包环境（有网环境下载 electron-builder 依赖）

### 6.4 验收方式
找一台干净的 Windows 10/11 电脑，从下载→解压→使用走一遍完整流程，全部 8 项通过

---

## 阶段依赖关系

```
Phase 1 (编辑器)
   ↓ 验收通过
Phase 2 (编译)
   ↓ 验收通过
Phase 3 (烧录)
   ↓ 验收通过
Phase 4 (串口监视器)
   ↓ 验收通过
Phase 5 (项目管理+模板)
   ↓ 验收通过
Phase 6 (打包发布)
```

**每个阶段严格验收，不跳阶段。**

## 验收流程

每个阶段完成后：
1. 开发者自测全部验收项，截图/录屏
2. 提交验收报告（通过/不通过 + 问题清单）
3. 不通过的问题修复后，重新验收
4. 全部通过后，git commit + 打 tag，进入下一阶段
