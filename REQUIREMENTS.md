# ESP32 便捷 IDE 方案 —— 需求与方案

## 一、项目背景

Arduino IDE 太简单、性能差；STM32CubeIDE/Keil 下载麻烦、工具链复杂。  
目标：**做一个面向新手的、开箱即用的 ESP32 IDE，国内用户零配置即可编译烧录。**

## 二、目标用户

- 嵌入式新手（学完 STC/STM32，想玩 ESP32）
- 国内用户（GitHub 下载困难，需要国内镜像或离线包）
- 零基础，不想折腾环境

## 三、核心需求

### 3.1 功能需求
| 功能 | 说明 |
|------|------|
| 代码编辑 | 语法高亮、自动补全、括号匹配 |
| 一键编译 | 点按钮即可编译，不需要命令行 |
| 一键烧录 | 选择串口，点按钮烧录 |
| 串口监视器 | 内嵌串口终端，查看输出/调试 |
| 项目模板 | 提供 ESP32-S3 基础模板（Blink、WiFi、串口等） |
| 芯片选择 | 支持 ESP32-S3（后续扩展 ESP32/ESP32-C3/ESP32-C6） |
| 编译输出 | 实时显示编译日志，错误可跳转到代码行 |

### 3.2 部署需求
| 需求 | 说明 |
|------|------|
| 离线可用 | 工具链预装，不依赖外网下载 |
| 国内分发 | Gitee Release / 百度网盘 / 蓝奏云 |
| 零安装 | 解压即用，不需要额外安装 Python 等依赖 |
| Windows 优先 | 目标平台 Windows 10/11（嵌入式开发者主流 OS） |
| 包体积 | 控制在 500MB 以内（含工具链） |

### 3.3 用户体验
- 第一次打开：引导选择芯片型号 + 串口号
- 新建项目：一键生成模板代码
- 编译/烧录：状态栏实时显示进度
- 报错处理：编译错误直接点击跳转到代码行

## 四、技术方案

### 4.1 架构
```
┌─────────────────────────────────────┐
│           IDE 前端 (Electron)        │
│  Monaco Editor + 自定义 UI          │
├─────────────────────────────────────┤
│           Node.js 后端              │
│  项目管理 | 编译调度 | 串口通信     │
├─────────────────────────────────────┤
│        PlatformIO Core (嵌入)       │
│  自动管理工具链、框架、编译烧录     │
├─────────────────────────────────────┤
│     ESP32 工具链 (预装离线包)       │
│  xtensa-esp-elf-gcc + esptool      │
└─────────────────────────────────────┘
```

### 4.2 技术栈
| 组件 | 选型 | 理由 |
|------|------|------|
| 前端框架 | Electron + React/Vue | 跨平台桌面应用 |
| 代码编辑器 | Monaco Editor | VS Code 同款，体验好 |
| 后端 | Node.js (Electron 主进程) | 与 Electron 天然集成 |
| 编译引擎 | PlatformIO Core (Python) | 成熟可靠，自动管理工具链 |
| 串口通信 | serialport (Node.js) | 跨平台串口库 |
| 打包分发 | electron-builder | 打包 Windows 安装包/压缩包 |

### 4.3 PlatformIO 集成方式
1. 将 PlatformIO Core 嵌入 Electron 应用
2. 通过 Node.js child_process 调用 PlatformIO CLI
3. 工具链预装在应用目录下，不走在线下载
4. `platformio.ini` 由 IDE 自动生成，用户无需手动配置

### 4.4 离线工具链方案
1. 预先在有网环境下载好 ESP32 工具链
2. 将 `~/.platformio/packages/` 中相关包打包
3. 应用首次启动时自动解压到本地目录
4. PlatformIO 配置 `packages_dir` 指向本地目录

## 五、开发计划

### Phase 1：最小可用版（MVP）—— 2 周
- [ ] Electron 项目脚手架
- [ ] Monaco Editor 集成（语法高亮、基础编辑）
- [ ] PlatformIO CLI 封装（编译/烧录/清理）
- [ ] ESP32-S3 项目模板
- [ ] 编译日志输出面板
- [ ] 串口选择 + 烧录功能

### Phase 2：体验优化 —— 1 周
- [ ] 串口监视器（内嵌终端）
- [ ] 错误跳转（点击编译错误定位代码行）
- [ ] 首次启动引导（选芯片、串口）
- [ ] 项目文件树管理

### Phase 3：打包分发 —— 1 周
- [ ] Windows 离线包制作（含工具链 + Python）
- [ ] electron-builder 打包
- [ ] 国内下载渠道发布（Gitee Release）
- [ ] 使用文档/教程

### Phase 4：扩展 —— 后续
- [ ] 支持 ESP32 / ESP32-C3 / ESP32-C6
- [ ] STC 系列支持（通过 SDCC）
- [ ] STM32 支持（通过 PlatformIO）
- [ ] 自动补全增强（ESP-IDF API 提示）

## 六、风险与对策

| 风险 | 对策 |
|------|------|
| PlatformIO 嵌入 Electron 兼容性 | 先做 POC 验证，确保 Python 调用正常 |
| 工具链包体积过大 | 只打包 ESP32-S3 必需组件，按需扩展 |
| Windows 权限问题 | 以用户级安装，避免管理员权限 |
| 国内分发文件大小限制 | 百度网盘/蓝奏云大文件分卷 |

## 七、参考项目

- Arduino IDE 2.x（Electron 架构参考）
- PlatformIO IDE（VS Code 扩展，功能参考）
- STC32 IDE（WebView2 架构参考）
- ESP-IDF VS Code 扩展（功能参考）
