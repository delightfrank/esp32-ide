# ESP32 便捷 IDE —— 完整方案

---

## 一、项目背景与目标

### 1.1 现状痛点

| IDE | 问题 |
|-----|------|
| Arduino IDE | 功能简陋、性能差、不支持高级调试 |
| STM32CubeIDE | 体积大、Java 吃内存、新手安装环境劝退 |
| Keil MDK | 需 license、仅 Windows、配置复杂 |
| PlatformIO CLI | 命令行门槛高、新手不友好 |
| ESP-IDF VS Code 扩展 | 依赖 VS Code 生态、工具链下载慢（GitHub） |

### 1.2 项目目标

做一个**面向国内新手的 ESP32 便捷 IDE**，满足：

- **开箱即用**：下载解压即可使用，零配置
- **离线可用**：工具链预装，不依赖外网
- **国内分发**：Gitee / 百度网盘 / 蓝奏云，下载无障碍
- **新手友好**：界面直观，一键编译烧录，不需要命令行知识
- **功能完整**：代码编辑 + 编译 + 烧录 + 串口监视器

### 1.3 目标用户

- 嵌入式新手（学完 STC/STM32，想玩 ESP32）
- 高校学生（毕设、课程设计、竞赛）
- 爱好者（想快速上手 ESP32，不想折腾环境）
- 国内用户（GitHub 下载困难）

---

## 二、功能需求

### 2.1 核心功能

| 功能模块 | 功能点 | 优先级 |
|----------|--------|--------|
| 代码编辑 | 语法高亮（C/C++） | P0 |
| | 自动补全（基础） | P1 |
| | 括号匹配 | P0 |
| | 代码缩进 | P0 |
| | 搜索替换 | P1 |
| 编译 | 一键编译 | P0 |
| | 编译日志实时输出 | P0 |
| | 编译错误跳转到代码行 | P0 |
| | 编译状态指示（成功/失败） | P0 |
| 烧录 | 串口自动检测 | P0 |
| | 一键烧录 | P0 |
| | 烧录进度显示 | P0 |
| | 烧录成功/失败提示 | P0 |
| 串口监视器 | 实时接收数据 | P0 |
| | 发送数据 | P0 |
| | 波特率选择 | P0 |
| | 清屏 | P0 |
| | 自动滚动 | P0 |
| 项目管理 | 新建项目向导 | P0 |
| | 项目模板 | P0 |
| | 文件树管理 | P0 |
| | 删除/重命名文件 | P1 |

### 2.2 部署需求

| 需求 | 说明 |
|------|------|
| 目标平台 | Windows 10/11（64-bit） |
| 安装方式 | 解压即用，绿色免安装 |
| 离线运行 | 工具链预装，编译烧录不需联网 |
| 包体大小 | ≤ 500MB（含全部工具链） |
| 分发渠道 | Gitee Release（主）、百度网盘（备）、蓝奏云（备） |
| 卸载方式 | 删除整个文件夹，无残留 |

### 2.3 用户体验

- 首次打开：引导选择芯片型号 + 串口号
- 新建项目：向导式操作（选芯片 → 选模板 → 命名 → 生成）
- 编译烧录：状态栏实时反馈进度
- 错误处理：编译报错直接点击跳转到代码行
- 暗色主题：默认暗色，护眼

---

## 三、技术架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                Electron 应用壳                   │
│                                                 │
│  ┌──────────────┐  ┌─────────────────────────┐  │
│  │  Monaco      │  │  UI 界面                │  │
│  │  Editor      │  │  • 菜单栏               │  │
│  │  (代码编辑)  │  │  • 工具栏（编译/烧录）  │  │
│  │              │  │  • 文件树               │  │
│  │              │  │  • 编译输出面板         │  │
│  │              │  │  • 串口监视器           │  │
│  │              │  │  • 状态栏               │  │
│  └──────────────┘  └─────────────────────────┘  │
│                                                 │
├─────────────────────────────────────────────────┤
│              Node.js 主进程                     │
│                                                 │
│  • 项目文件管理 (fs 模块)                       │
│  • PlatformIO 调用 (child_process)              │
│  • 串口通信 (serialport)                        │
│  • 配置管理 (platformio.ini 自动生成)           │
│                                                 │
├─────────────────────────────────────────────────┤
│           PlatformIO Core (Python)              │
│                                                 │
│  • pio run — 编译                               │
│  • pio run -t upload — 烧录                     │
│  • pio device monitor — 串口监视                │
│  • pio init — 项目初始化                        │
│                                                 │
├─────────────────────────────────────────────────┤
│         本地预装工具链（无需联网）               │
│                                                 │
│  • xtensa-esp-elf-gcc 14.2.0 (编译器)          │
│  • esptool 5.3.0 (烧录工具)                    │
│  • Arduino ESP32 3.3.9 (框架)                  │
│  • Python embedded (PlatformIO 运行时)          │
└─────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 组件 | 选型 | 版本 | 理由 |
|------|------|------|------|
| 桌面框架 | Electron | 33+ | 跨平台，生态成熟 |
| 代码编辑器 | Monaco Editor | latest | VS Code 同款，体验好 |
| UI 框架 | React | 18+ | 组件化，生态丰富 |
| 后端运行时 | Node.js (Electron 内置) | 22+ | 与 Electron 天然集成 |
| 编译引擎 | PlatformIO Core | 6.1.19 | 成熟可靠，自动管理工具链 |
| 串口通信 | serialport | 12+ | 跨平台串口库 |
| 打包工具 | electron-builder | latest | 打包 Windows 安装包/压缩包 |

### 3.3 目录结构

```
esp32-ide/
├── src/
│   ├── main/                          # 主进程
│   │   ├── index.js                   # 入口，创建窗口
│   │   ├── pio-bridge.js              # PlatformIO CLI 封装
│   │   ├── serial-manager.js          # 串口管理
│   │   ├── project-manager.js         # 项目文件管理
│   │   └── config.js                  # 配置管理
│   ├── renderer/                      # 渲染进程
│   │   ├── index.html                 # 主页面
│   │   ├── App.jsx                    # 根组件
│   │   ├── components/
│   │   │   ├── Editor.jsx             # Monaco Editor 封装
│   │   │   ├── Toolbar.jsx            # 工具栏（编译/烧录按钮）
│   │   │   ├── FileTree.jsx           # 文件树
│   │   │   ├── ConsolePanel.jsx       # 编译输出/串口监视
│   │   │   ├── StatusBar.jsx          # 状态栏
│   │   │   └── SetupWizard.jsx        # 首次启动引导
│   │   └── styles/
│   │       └── dark.css               # 暗色主题
│   └── templates/                     # 项目模板
│       ├── blink/
│       │   ├── main.cpp
│       │   └── platformio.ini.template
│       ├── wifi-scan/
│       │   └── main.cpp
│       └── serial-echo/
│           └── main.cpp
├── tools/                             # 工具脚本
│   ├── download-toolchain.js          # 下载工具链（打包用）
│   └── package-offline.js             # 打包离线包
├── package.json
├── electron-builder.yml               # 打包配置
├── vite.config.js                     # 前端构建配置
└── build/                             # 打包输出目录
```

---

## 四、分阶段实施方案

> **核心原则：每阶段完成 → 验收测试 → 确认无 bug → 再进入下一阶段**

### 阶段依赖关系

```
Phase 1 (编辑器基础)
   ↓ 验收通过
Phase 2 (编译功能)
   ↓ 验收通过
Phase 3 (烧录功能)
   ↓ 验收通过
Phase 4 (串口监视器)
   ↓ 验收通过
Phase 5 (项目管理+模板)
   ↓ 验收通过
Phase 6 (打包发布)
```

---

### Phase 1：Electron 脚手架 + 编辑器基础

**目标：** 跑起来一个 Electron 应用，能打开、编辑、保存代码文件

**预计工期：** 3 天

#### 交付物

1. Electron 项目初始化完成（Vite + React）
2. Monaco Editor 集成，支持 C/C++ 语法高亮
3. 基础菜单栏（文件：新建/打开/保存/另存为）
4. 窗口标题显示当前文件名
5. 暗色主题基础样式

#### 验收标准

| # | 测试项 | 通过条件 | 验证方法 |
|---|--------|----------|----------|
| 1 | 应用启动 | 双击 exe 后 3 秒内显示窗口，无白屏 | 手动双击，计时 |
| 2 | 新建文件 | Ctrl+N 新建空白文件，可输入代码 | 手动操作，截图 |
| 3 | 打开文件 | Ctrl+O 选择 .c/.cpp/.h 文件，内容正确显示 | 准备测试文件，打开验证 |
| 4 | 保存文件 | Ctrl+S 保存，文件内容与编辑器一致 | 保存后用其他编辑器对比 |
| 5 | 另存为 | Ctrl+Shift+S 另存为新路径，内容正确 | 验证新文件内容 |
| 6 | 语法高亮 | 输入 C 代码，`#include`/`int`/`return` 等关键字有颜色 | 截图 |
| 7 | 窗口行为 | 可拖动、可缩放、最小化/最大化/关闭正常 | 手动操作 |
| 8 | 暗色主题 | 编辑器背景为深色，文字清晰可读 | 截图 |
| 9 | 无崩溃 | 连续操作 10 分钟（打开/关闭/编辑/保存），无闪退 | 计时操作 |

#### 验收流程

1. 开发者完成全部交付物
2. 自测 9 项验收标准，截图/录屏
3. 提交验收报告（通过/不通过 + 问题清单）
4. 不通过：修复 → 重新验收
5. 全部通过：git commit + tag `v0.1.0-editor` → 进入 Phase 2

---

### Phase 2：PlatformIO 集成 + 编译功能

**目标：** 能在 IDE 内一键编译 ESP32-S3 项目

**预计工期：** 4 天

**前置条件：** Phase 1 验收通过

#### 交付物

1. PlatformIO Core 嵌入（通过 Node.js child_process 调用）
2. `pio-bridge.js` 封装：编译/清理/状态查询
3. 编译按钮 + 编译输出面板
4. 自动根据芯片选择生成 `platformio.ini`
5. 编译日志实时显示（逐行推送）
6. 编译成功/失败状态提示
7. 编译错误跳转到代码行

#### 验收标准

| # | 测试项 | 通过条件 | 验证方法 |
|---|--------|----------|----------|
| 1 | PlatformIO 可调用 | 点击编译后，后端正确执行 `pio run` | 查看日志输出 |
| 2 | 自动生成配置 | 选择 ESP32-S3 后，`platformio.ini` 内容正确（board、framework、monitor_speed） | 打开文件检查 |
| 3 | 编译日志 | 输出面板实时显示编译日志，不卡顿，不丢失行 | 观察日志滚动 |
| 4 | 编译成功 | Blink 模板项目编译通过，显示 "SUCCESS" | 查看输出 |
| 5 | 编译失败 | 故意写错代码（如缺少分号），显示错误行号+原因 | 修改代码后编译 |
| 6 | 错误跳转 | 点击编译错误中的行号，光标跳转到代码对应行 | 点击测试 |
| 7 | 编译按钮状态 | 编译中按钮变灰 + 显示"编译中..."，完成后恢复 | 观察按钮变化 |
| 8 | 编译清理 | 调用 `pio run -t clean`，build 目录被清空 | 检查目录 |
| 9 | 无残留进程 | 编译完成后无残留 `pio`/`gcc` 进程 | `tasklist` 检查 |
| 10 | 编译耗时 | ESP32-S3 Blink 项目编译 < 60 秒 | 计时 |

#### 验收流程

1. 准备一个 Blink 模板项目
2. 自测 10 项验收标准
3. 提交验收报告
4. 不通过：修复 → 重新验收
5. 全部通过：git commit + tag `v0.2.0-build` → 进入 Phase 3

---

### Phase 3：烧录功能

**目标：** 能一键烧录到 ESP32-S3 开发板

**预计工期：** 3 天

**前置条件：** Phase 2 验收通过 + ESP32-S3 开发板 + USB 线

#### 交付物

1. 串口自动检测（扫描可用 COM 口）
2. 串口选择下拉菜单
3. 烧录按钮
4. 烧录进度显示（百分比/进度条）
5. 烧录成功/失败弹窗提示
6. 烧录前自动编译（如未编译）
7. 烧录日志输出

#### 验收标准

| # | 测试项 | 通过条件 | 验证方法 |
|---|--------|----------|----------|
| 1 | 串口检测 | 连接开发板后，下拉列表自动显示可用串口 | 插入 USB 后查看 |
| 2 | 串口刷新 | 点击刷新按钮，重新扫描串口 | 插拔 USB 后刷新 |
| 3 | 烧录执行 | 点击烧录，调用 `pio run -t upload` | 查看日志 |
| 4 | 烧录成功 | 开发板 LED 按 Blink 程序闪烁 | 目视观察 |
| 5 | 烧录失败（无板） | 拔掉 USB 后烧录，显示"未找到串口"错误 | 拔线后测试 |
| 6 | 烧录失败（占用） | 其他程序占用串口时烧录，提示"串口被占用" | 打开串口助手占用后测试 |
| 7 | 进度显示 | 烧录过程中显示百分比或进度条 | 观察 UI |
| 8 | 自动编译 | 修改代码后直接烧录，自动先编译再烧录 | 修改后点烧录 |
| 9 | 完整流程 | 新建项目 → 写 Blink 代码 → 编译 → 烧录 → LED 闪烁 | 全流程操作 |
| 10 | 烧录耗时 | Blink 项目烧录 < 10 秒 | 计时 |

#### 验收流程

1. 准备 ESP32-S3 开发板 + USB 线
2. 自测 10 项验收标准，录屏
3. 提交验收报告
4. 不通过：修复 → 重新验收
5. 全部通过：git commit + tag `v0.3.0-upload` → 进入 Phase 4

---

### Phase 4：串口监视器

**目标：** 能在 IDE 内查看串口输出、发送数据

**预计工期：** 3 天

**前置条件：** Phase 3 验收通过 + 开发板运行串口回显程序

#### 交付物

1. 串口监视器面板（底部 tab，与编译输出共用区域）
2. 实时显示串口数据（支持 UTF-8 + HEX 显示切换）
3. 数据发送输入框 + 发送按钮
4. 波特率选择下拉（9600/57600/115200/自定义）
5. 清屏按钮
6. 自动滚动（新数据到来自动滚到底部，可锁定）
7. 连接/断开按钮 + 状态指示

#### 验收标准

| # | 测试项 | 通过条件 | 验证方法 |
|---|--------|----------|----------|
| 1 | 打开串口 | 选择串口+波特率后点击连接，状态显示"已连接" | 操作 + 状态栏 |
| 2 | 接收数据 | 开发板发送数据，监视器实时显示 | 开发板发 echo 测试 |
| 3 | 发送数据 | 输入框输入文字+回车，开发板收到并回显 | 输入后查看回显 |
| 4 | 波特率切换 | 115200→9600 切换后，数据接收正常 | 切换后测试 |
| 5 | HEX 显示 | 切换 HEX 模式，数据显示为十六进制 | 输入 ASCII 文本查看 |
| 6 | 断开连接 | 点击断开，串口释放，其他程序可正常打开 | 断开后用串口助手验证 |
| 7 | 清屏 | 点击清屏，历史数据清除，新数据继续显示 | 清屏后发送数据 |
| 8 | 自动滚动 | 新数据到来时自动滚动到底部 | 发送大量数据观察 |
| 9 | 滚动锁定 | 滚动条上移后，新数据不再自动滚动到底部 | 上移滚动条后发数据 |
| 10 | 与烧录共存 | 串口监视器打开时，烧录功能正常（自动断开→烧录→重连） | 打开监视器后烧录 |

#### 验收流程

1. 开发板运行串口回显程序（Serial echo）
2. 自测 10 项验收标准
3. 提交验收报告
4. 不通过：修复 → 重新验收
5. 全部通过：git commit + tag `v0.4.0-serial` → 进入 Phase 5

---

### Phase 5：项目管理 + 模板

**目标：** 新手友好，一键创建项目，文件管理清晰

**预计工期：** 3 天

**前置条件：** Phase 4 验收通过

#### 交付物

1. 左侧文件树面板（可折叠）
2. 新建项目向导（选芯片 → 选模板 → 命名 → 生成）
3. 项目模板：
   - Blink（LED 闪烁）
   - WiFi Scanner（扫描周围 WiFi）
   - Serial Echo（串口回显）
4. 右键菜单：新建文件/文件夹、删除、重命名
5. 文件图标区分（.c/.cpp/.h/.ino）
6. 刷新按钮（同步外部修改）

#### 验收标准

| # | 测试项 | 通过条件 | 验证方法 |
|---|--------|----------|----------|
| 1 | 文件树显示 | 左侧正确显示项目目录结构 | 观察 UI |
| 2 | 新建项目 | 向导走完，生成完整项目目录 + platformio.ini | 检查目录结构 |
| 3 | 模板-Blink | Blink 模板编译通过 + 烧录后 LED 闪烁 | 编译烧录验证 |
| 4 | 模板-WiFi | WiFi Scanner 模板编译通过 + 烧录后串口输出 AP 列表 | 编译烧录验证 |
| 5 | 模板-Serial | Serial Echo 模板编译通过 + 串口收发正常 | 编译烧录验证 |
| 6 | 新建文件 | 右键新建 .c 文件，文件树显示，编译识别 | 新建后编译 |
| 7 | 删除文件 | 右键删除文件，文件树更新，编译不再包含 | 删除后编译 |
| 8 | 重命名 | 右键重命名文件，内容不变 | 重命名后打开查看 |
| 9 | 多文件项目 | 新建 .h 文件 + #include，编译通过 | 头文件引用测试 |
| 10 | 刷新 | 手动在外部修改文件，点刷新后文件树更新 | 外部修改后刷新 |

#### 验收流程

1. 自测 10 项验收标准
2. 提交验收报告
3. 不通过：修复 → 重新验收
4. 全部通过：git commit + tag `v0.5.0-projects` → 进入 Phase 6

---

### Phase 6：打包发布

**目标：** 打包成 Windows 离线安装包，国内可下载

**预计工期：** 3 天

**前置条件：** Phase 1-5 全部验收通过

#### 交付物

1. Windows 离线压缩包（含全部依赖）
2. 使用说明文档（README.md）
3. 常见问题 FAQ
4. Gitee Release 发布页面
5. 百度网盘 / 蓝奏云备份链接

#### 离线包内容

```
ESP32-IDE-v1.0.zip
├── ESP32-IDE.exe              # 主程序（~80MB）
├── resources/                 # Electron 资源
├── python/                    # Python embedded（~30MB）
├── platformio/                # PlatformIO Core（~30MB）
├── packages/                  # 工具链 + 框架
│   ├── toolchain-xtensa-esp-elf/  # 编译器（~200MB）
│   ├── tool-esptoolpy/            # 烧录工具（~15MB）
│   └── framework-arduinoespressif32/  # 框架（~100MB）
├── templates/                 # 项目模板
├── README.txt                 # 快速开始
└── docs/                      # 详细文档
```

#### 验收标准

| # | 测试项 | 通过条件 | 验证方法 |
|---|--------|----------|----------|
| 1 | 包体大小 | ≤ 500MB | 查看文件属性 |
| 2 | 无联网依赖 | 断网状态下解压→启动→编译→烧录全流程正常 | 拔网线测试 |
| 3 | 全新机器 | 在未装任何开发环境的 Windows 10/11 上测试通过 | 找一台干净电脑 |
| 4 | 下载速度 | 国内网络下 Gitee 下载速度 ≥ 1MB/s | 实测下载 |
| 5 | 解压即用 | 解压后双击 exe 直接运行，不需安装 Python 等 | 直接运行 |
| 6 | 编译烧录 | 离线包中 Blink 模板编译+烧录成功 | 离线全流程 |
| 7 | 文档完整 | README 包含：功能介绍、截图、使用教程、常见问题 | 阅读检查 |
| 8 | 卸载 | 删除整个文件夹即卸载，无注册表/环境变量残留 | 删除后检查 |
| 9 | 分发渠道 | Gitee Release 页面可正常访问和下载 | 访问验证 |
| 10 | 无杀软误报 | Windows Defender 不报毒 | 上传 VirusTotal 检查 |

#### 验收流程

1. 找一台干净 Windows 10/11 电脑
2. 从下载→解压→使用走完整流程
3. 自测 10 项验收标准，录屏
4. 提交验收报告
5. 不通过：修复 → 重新验收
6. 全部通过：git commit + tag `v1.0.0-release` → 发布

---

## 五、验收管理规范

### 5.1 验收流程

```
开发完成
   ↓
自测全部验收项（截图/录屏）
   ↓
提交验收报告
   ↓
  ┌─ 全部通过 → git commit + tag → 进入下一阶段
  └─ 未通过   → 修复 → 重新验收
```

### 5.2 验收报告模板

```markdown
# Phase X 验收报告

## 测试环境
- 操作系统：Windows 10/11
- 测试日期：YYYY-MM-DD
- 测试人：XXX

## 验收结果
| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | xxx | ✅/❌ | |
| 2 | xxx | ✅/❌ | |

## 问题清单
| # | 问题描述 | 严重程度 | 修复状态 |
|---|----------|----------|----------|
| 1 | xxx | P0/P1/P2 | 已修复/待修复 |

## 结论
- [ ] 全部通过，进入下一阶段
- [ ] 存在未修复问题，需重新验收
```

### 5.3 版本号规则

| 阶段 | Tag | 版本号 |
|------|-----|--------|
| Phase 1 完成 | v0.1.0-editor | 0.1.0 |
| Phase 2 完成 | v0.2.0-build | 0.2.0 |
| Phase 3 完成 | v0.3.0-upload | 0.3.0 |
| Phase 4 完成 | v0.4.0-serial | 0.4.0 |
| Phase 5 完成 | v0.5.0-projects | 0.5.0 |
| Phase 6 完成 | v1.0.0-release | 1.0.0 |

---

## 六、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| PlatformIO 嵌入 Electron 兼容性 | 编译/烧录功能不可用 | Phase 2 做 POC 验证，确认 child_process 调用正常 |
| 工具链包体积过大 | 超过 500MB 限制 | 只打包 ESP32-S3 必需组件，用 xz 压缩 |
| Windows 权限问题 | 安装/运行失败 | 以用户级运行，路径不含中文和空格 |
| 国内分发文件大小 | 网盘上传限制 | 百度网盘分卷压缩，蓝奏云分文件上传 |
| 杀软误报 | 用户不敢使用 | 使用正式代码签名证书（需购买） |
| 串口驱动问题 | 烧录失败 | 文档中说明需要安装 CH340/CP2102 驱动 |
| PlatformIO 版本兼容 | 工具链下载失败 | 锁定 PlatformIO + 工具链版本，不自动更新 |

---

## 七、后续扩展（Phase 7+）

| 功能 | 说明 |
|------|------|
| ESP32/ESP32-C3/C6 支持 | 增加芯片选项 |
| STC 系列支持 | 集成 SDCC 编译器 |
| STM32 支持 | 集成 arm-none-eabi-gcc |
| 增强自动补全 | ESP-IDF API 提示 |
| 代码片段 | 内置常用代码模板 |
| Git 集成 | 基础版本控制 |
| 主题切换 | 支持亮色/暗色主题切换 |
| 多语言 | 界面中英文切换 |

---

## 八、参考项目

- Arduino IDE 2.x —— Electron 架构参考
- PlatformIO IDE (VS Code 扩展) —— 功能参考
- STC32 IDE —— WebView2 壳子参考
- ESP-IDF VS Code 扩展 —— 功能参考
- Theia IDE —— 开源 IDE 框架参考
