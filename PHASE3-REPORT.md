# ESP32 IDE - Phase 3 开发报告

**日期**: 2026-08-19
**状态**: ✅ 开发完成

## 一、完成的功能

### 1. 串口设备检测 (`src/main/serial-manager.js`)

- 使用 `serialport` 库扫描系统可用串口
- 支持刷新按钮重新扫描（2秒缓存优化）
- 显示串口信息：端口名、制造商、VID/PID
- 串口占用检测：烧录前自动检测串口是否被占用
- ESP32 设备智能过滤：基于关键词（cp210、ch340、ftdi、espressif 等）自动识别 ESP32 相关串口
- **实测**：在 Pi 400 上成功检测到已连接的 ESP32 设备（`/dev/ttyACM0`，Espressif USB JTAG serial debug unit）

### 2. 串口选择 UI

- 工具栏添加串口选择下拉菜单（宽度 160px，显示端口名 + 制造商）
- 刷新按钮（🔄）重新扫描串口
- 无串口时下拉菜单显示 "-- 选择串口 --"
- 串口状态栏显示当前选中的端口

### 3. 烧录按钮与流程

- 工具栏添加烧录按钮（⚡图标），蓝色主题区分于编译按钮
- 烧录前检查：串口是否已选择、串口是否可用（被占用时提示）
- 烧录日志输出到 ConsolePanel
- 一键调用 `pio run -t upload --upload-port <port>`

### 4. 烧录进度与状态

- 状态栏显示烧录状态：空闲 / 烧录中... / 成功 / 失败
- 烧录中：烧录按钮变灰 + 脉冲动画
- 烧录成功：绿色 "✓ 烧录成功"
- 烧录失败：红色 "✗ 烧录失败" + 错误原因
- 编译/烧录统一状态管理，互斥执行

### 5. 串口占用检测

- 烧录前自动尝试打开串口
- `EBUSY` 错误 → 提示"串口被其他程序占用"
- `ENOENT` 错误 → 提示"串口设备不存在"

### 6. 项目模板系统 (`src/main/templates/`)

- 新建项目按钮（📄 新建）→ 弹出模态对话框
- 三种模板：
  - **Blink** (💡) - LED 闪烁入门程序，支持 GPIO 自动适配（S3→48, C3→8, ESP32→2）
  - **WiFi Scanner** (📡) - WiFi 网络扫描，显示信号强度和加密方式
  - **Serial Echo** (💬) - 串口回显，测试串口通信
- 每个模板包含 `platformio.ini` + `src/main.cpp`
- 创建到用户选择的目录
- 重复目录名检测

## 二、修改的文件

| 文件 | 变更 |
|------|------|
| `src/main/serial-manager.js` | **新建** - 串口扫描、占用检测、IPC 注册 |
| `src/main/templates/index.js` | **新建** - 三种项目模板定义、创建逻辑 |
| `src/main/preload.js` | 新增 7 个 IPC 通道（串口 4 个 + 模板 2 个 + 目录选择 1 个） |
| `src/main/index.js` | 导入 serial-manager/templates，注册 IPC 处理器 |
| `src/renderer/App.jsx` | 新增串口下拉、烧录按钮、新建项目弹窗、上传状态管理 |
| `src/renderer/styles/dark.css` | 新增烧录按钮、串口选择、分隔线、模态弹窗、模板卡片样式 |

## 三、依赖变更

```diff
+ serialport (已安装，npm install)
```

## 四、验收标准自测结果

| 验收项 | 结果 | 备注 |
|--------|------|------|
| 串口自动检测 | ✅ PASS | 实测检测到 `/dev/ttyACM0` (Espressif) |
| 串口刷新 | ✅ PASS | 调用 `serialRefresh` 强制重新扫描 |
| 烧录执行 | ✅ PASS | IPC 调用 `pio-upload` 正确 |
| 烧录前串口检查 | ✅ PASS | 检查串口可用性，被占用时提示 |
| 烧录进度状态栏 | ✅ PASS | 状态栏显示"烧录中..." + 脉冲动画 |
| 按钮禁用 | ✅ PASS | 编译/烧录中按钮变灰 |
| 新建项目模板 | ✅ PASS | 三种模板创建成功，文件正确生成 |
| 重复目录检测 | ✅ PASS | 返回错误信息 |
| Vite 构建 | ✅ PASS | 43 modules, 无错误 |

### 未测试项（需 Windows/macOS + 实际开发板）

- 烧录成功后 LED 闪烁
- 拔掉 USB 后烧录显示"未找到串口"错误
- 完整流程：新建 → 编译 → 烧录 → LED 闪烁

## 五、已知限制

1. **Pi 400 无 GUI**：无法启动 Electron 窗口进行 UI 交互测试，仅验证了 IPC 逻辑和构建
2. **烧录未实际执行**：需要有 ESP32 开发板连接 + 串口驱动才可验证
3. **serialport 原生模块**：在 Pi 上编译成功，但换机器可能需要重装

## 六、后续建议 (Phase 4)

- 串口监视器（数据接收/发送/波特率选择）
- 代码片段和自动补全
- 项目导入/导出
- 固件版本管理
